export * from '../models/AddFirmwareResponse.js';
export * from '../models/BodyCreateUserFromInviteApiUsersRegisterPost.js';
export * from '../models/BodyRequestPasswordResetApiForgotPasswordPost.js';
export * from '../models/BodyResetPasswordApiResetPasswordPost.js';
export * from '../models/CollectionSchema.js';
export * from '../models/ConfigResponse.js';
export * from '../models/CustomLimitOffsetPageSimpleRomSchema.js';
export * from '../models/DetailedRomSchema.js';
export * from '../models/EarnedAchievement.js';
export * from '../models/EmulationDict.js';
export * from '../models/FilesystemDict.js';
export * from '../models/FirmwareSchema.js';
export * from '../models/FrontendDict.js';
export * from '../models/HTTPValidationError.js';
export * from '../models/HeartbeatResponse.js';
export * from '../models/IGDBAgeRating.js';
export * from '../models/IGDBMetadataPlatform.js';
export * from '../models/IGDBRelatedGame.js';
export * from '../models/InviteLinkSchema.js';
export * from '../models/MessageResponse.js';
export * from '../models/MetadataSourcesDict.js';
export * from '../models/MobyMetadataPlatform.js';
export * from '../models/OIDCDict.js';
export * from '../models/PlatformSchema.js';
export * from '../models/RAGameRomAchievement.js';
export * from '../models/RAProgression.js';
export * from '../models/RAUserGameProgression.js';
export * from '../models/Role.js';
export * from '../models/RomFileCategory.js';
export * from '../models/RomFileSchema.js';
export * from '../models/RomIGDBMetadata.js';
export * from '../models/RomMetadataSchema.js';
export * from '../models/RomMobyMetadata.js';
export * from '../models/RomRAMetadata.js';
export * from '../models/RomSSMetadata.js';
export * from '../models/RomUserSchema.js';
export * from '../models/RomUserStatus.js';
export * from '../models/SaveSchema.js';
export * from '../models/SchedulerDict.js';
export * from '../models/ScreenshotSchema.js';
export * from '../models/SearchCoverSchema.js';
export * from '../models/SearchRomSchema.js';
export * from '../models/SiblingRomSchema.js';
export * from '../models/SimpleRomSchema.js';
export * from '../models/StateSchema.js';
export * from '../models/StatsReturn.js';
export * from '../models/SystemDict.js';
export * from '../models/TaskDict.js';
export * from '../models/TinfoilFeedFileSchema.js';
export * from '../models/TinfoilFeedSchema.js';
export * from '../models/TinfoilFeedTitleDBSchema.js';
export * from '../models/TokenResponse.js';
export * from '../models/UserNotesSchema.js';
export * from '../models/UserSchema.js';
export * from '../models/ValidationError.js';
export * from '../models/ValidationErrorLocInner.js';
export * from '../models/VirtualCollectionSchema.js';
export * from '../models/WatcherDict.js';
export * from '../models/WebrcadeFeedCategorySchema.js';
export * from '../models/WebrcadeFeedItemPropsSchema.js';
export * from '../models/WebrcadeFeedItemSchema.js';
export * from '../models/WebrcadeFeedSchema.js';

import { AddFirmwareResponse } from '../models/AddFirmwareResponse.js';
import { BodyCreateUserFromInviteApiUsersRegisterPost } from '../models/BodyCreateUserFromInviteApiUsersRegisterPost.js';
import { BodyRequestPasswordResetApiForgotPasswordPost } from '../models/BodyRequestPasswordResetApiForgotPasswordPost.js';
import { BodyResetPasswordApiResetPasswordPost } from '../models/BodyResetPasswordApiResetPasswordPost.js';
import { CollectionSchema } from '../models/CollectionSchema.js';
import { ConfigResponse } from '../models/ConfigResponse.js';
import { CustomLimitOffsetPageSimpleRomSchema } from '../models/CustomLimitOffsetPageSimpleRomSchema.js';
import { DetailedRomSchema } from '../models/DetailedRomSchema.js';
import { EarnedAchievement } from '../models/EarnedAchievement.js';
import { EmulationDict } from '../models/EmulationDict.js';
import { FilesystemDict } from '../models/FilesystemDict.js';
import { FirmwareSchema } from '../models/FirmwareSchema.js';
import { FrontendDict } from '../models/FrontendDict.js';
import { HTTPValidationError } from '../models/HTTPValidationError.js';
import { HeartbeatResponse } from '../models/HeartbeatResponse.js';
import { IGDBAgeRating } from '../models/IGDBAgeRating.js';
import { IGDBMetadataPlatform } from '../models/IGDBMetadataPlatform.js';
import { IGDBRelatedGame } from '../models/IGDBRelatedGame.js';
import { InviteLinkSchema } from '../models/InviteLinkSchema.js';
import { MessageResponse } from '../models/MessageResponse.js';
import { MetadataSourcesDict } from '../models/MetadataSourcesDict.js';
import { MobyMetadataPlatform } from '../models/MobyMetadataPlatform.js';
import { OIDCDict } from '../models/OIDCDict.js';
import { PlatformSchema } from '../models/PlatformSchema.js';
import { RAGameRomAchievement } from '../models/RAGameRomAchievement.js';
import { RAProgression } from '../models/RAProgression.js';
import { RAUserGameProgression } from '../models/RAUserGameProgression.js';
import { Role } from '../models/Role.js';
import { RomFileCategory } from '../models/RomFileCategory.js';
import { RomFileSchema              } from '../models/RomFileSchema.js';
import { RomIGDBMetadata } from '../models/RomIGDBMetadata.js';
import { RomMetadataSchema } from '../models/RomMetadataSchema.js';
import { RomMobyMetadata } from '../models/RomMobyMetadata.js';
import { RomRAMetadata } from '../models/RomRAMetadata.js';
import { RomSSMetadata } from '../models/RomSSMetadata.js';
import { RomUserSchema                  } from '../models/RomUserSchema.js';
import { RomUserStatus } from '../models/RomUserStatus.js';
import { SaveSchema } from '../models/SaveSchema.js';
import { SchedulerDict } from '../models/SchedulerDict.js';
import { ScreenshotSchema } from '../models/ScreenshotSchema.js';
import { SearchCoverSchema } from '../models/SearchCoverSchema.js';
import { SearchRomSchema } from '../models/SearchRomSchema.js';
import { SiblingRomSchema } from '../models/SiblingRomSchema.js';
import { SimpleRomSchema } from '../models/SimpleRomSchema.js';
import { StateSchema } from '../models/StateSchema.js';
import { StatsReturn } from '../models/StatsReturn.js';
import { SystemDict } from '../models/SystemDict.js';
import { TaskDict } from '../models/TaskDict.js';
import { TinfoilFeedFileSchema } from '../models/TinfoilFeedFileSchema.js';
import { TinfoilFeedSchema } from '../models/TinfoilFeedSchema.js';
import { TinfoilFeedTitleDBSchema } from '../models/TinfoilFeedTitleDBSchema.js';
import { TokenResponse } from '../models/TokenResponse.js';
import { UserNotesSchema } from '../models/UserNotesSchema.js';
import { UserSchema              } from '../models/UserSchema.js';
import { ValidationError } from '../models/ValidationError.js';
import { ValidationErrorLocInner } from '../models/ValidationErrorLocInner.js';
import { VirtualCollectionSchema } from '../models/VirtualCollectionSchema.js';
import { WatcherDict } from '../models/WatcherDict.js';
import { WebrcadeFeedCategorySchema } from '../models/WebrcadeFeedCategorySchema.js';
import { WebrcadeFeedItemPropsSchema } from '../models/WebrcadeFeedItemPropsSchema.js';
import { WebrcadeFeedItemSchema } from '../models/WebrcadeFeedItemSchema.js';
import { WebrcadeFeedSchema } from '../models/WebrcadeFeedSchema.js';

/* tslint:disable:no-unused-variable */
let primitives = [
                    "string",
                    "boolean",
                    "double",
                    "integer",
                    "long",
                    "float",
                    "number",
                    "any"
                 ];

let enumsMap: Set<string> = new Set<string>([
    "Role",
    "RomFileCategory",
    "RomUserStatus",
]);

let typeMap: {[index: string]: any} = {
    "AddFirmwareResponse": AddFirmwareResponse,
    "BodyCreateUserFromInviteApiUsersRegisterPost": BodyCreateUserFromInviteApiUsersRegisterPost,
    "BodyRequestPasswordResetApiForgotPasswordPost": BodyRequestPasswordResetApiForgotPasswordPost,
    "BodyResetPasswordApiResetPasswordPost": BodyResetPasswordApiResetPasswordPost,
    "CollectionSchema": CollectionSchema,
    "ConfigResponse": ConfigResponse,
    "CustomLimitOffsetPageSimpleRomSchema": CustomLimitOffsetPageSimpleRomSchema,
    "DetailedRomSchema": DetailedRomSchema,
    "EarnedAchievement": EarnedAchievement,
    "EmulationDict": EmulationDict,
    "FilesystemDict": FilesystemDict,
    "FirmwareSchema": FirmwareSchema,
    "FrontendDict": FrontendDict,
    "HTTPValidationError": HTTPValidationError,
    "HeartbeatResponse": HeartbeatResponse,
    "IGDBAgeRating": IGDBAgeRating,
    "IGDBMetadataPlatform": IGDBMetadataPlatform,
    "IGDBRelatedGame": IGDBRelatedGame,
    "InviteLinkSchema": InviteLinkSchema,
    "MessageResponse": MessageResponse,
    "MetadataSourcesDict": MetadataSourcesDict,
    "MobyMetadataPlatform": MobyMetadataPlatform,
    "OIDCDict": OIDCDict,
    "PlatformSchema": PlatformSchema,
    "RAGameRomAchievement": RAGameRomAchievement,
    "RAProgression": RAProgression,
    "RAUserGameProgression": RAUserGameProgression,
    "RomFileSchema": RomFileSchema,
    "RomIGDBMetadata": RomIGDBMetadata,
    "RomMetadataSchema": RomMetadataSchema,
    "RomMobyMetadata": RomMobyMetadata,
    "RomRAMetadata": RomRAMetadata,
    "RomSSMetadata": RomSSMetadata,
    "RomUserSchema": RomUserSchema,
    "SaveSchema": SaveSchema,
    "SchedulerDict": SchedulerDict,
    "ScreenshotSchema": ScreenshotSchema,
    "SearchCoverSchema": SearchCoverSchema,
    "SearchRomSchema": SearchRomSchema,
    "SiblingRomSchema": SiblingRomSchema,
    "SimpleRomSchema": SimpleRomSchema,
    "StateSchema": StateSchema,
    "StatsReturn": StatsReturn,
    "SystemDict": SystemDict,
    "TaskDict": TaskDict,
    "TinfoilFeedFileSchema": TinfoilFeedFileSchema,
    "TinfoilFeedSchema": TinfoilFeedSchema,
    "TinfoilFeedTitleDBSchema": TinfoilFeedTitleDBSchema,
    "TokenResponse": TokenResponse,
    "UserNotesSchema": UserNotesSchema,
    "UserSchema": UserSchema,
    "ValidationError": ValidationError,
    "ValidationErrorLocInner": ValidationErrorLocInner,
    "VirtualCollectionSchema": VirtualCollectionSchema,
    "WatcherDict": WatcherDict,
    "WebrcadeFeedCategorySchema": WebrcadeFeedCategorySchema,
    "WebrcadeFeedItemPropsSchema": WebrcadeFeedItemPropsSchema,
    "WebrcadeFeedItemSchema": WebrcadeFeedItemSchema,
    "WebrcadeFeedSchema": WebrcadeFeedSchema,
}

type MimeTypeDescriptor = {
    type: string;
    subtype: string;
    subtypeTokens: string[];
};

/**
 * Every mime-type consists of a type, subtype, and optional parameters.
 * The subtype can be composite, including information about the content format.
 * For example: `application/json-patch+json`, `application/merge-patch+json`.
 *
 * This helper transforms a string mime-type into an internal representation.
 * This simplifies the implementation of predicates that in turn define common rules for parsing or stringifying
 * the payload.
 */
const parseMimeType = (mimeType: string): MimeTypeDescriptor => {
    const [type = '', subtype = ''] = mimeType.split('/');
    return {
        type,
        subtype,
        subtypeTokens: subtype.split('+'),
    };
};

type MimeTypePredicate = (mimeType: string) => boolean;

// This factory creates a predicate function that checks a string mime-type against defined rules.
const mimeTypePredicateFactory = (predicate: (descriptor: MimeTypeDescriptor) => boolean): MimeTypePredicate => (mimeType) => predicate(parseMimeType(mimeType));

// Use this factory when you need to define a simple predicate based only on type and, if applicable, subtype.
const mimeTypeSimplePredicateFactory = (type: string, subtype?: string): MimeTypePredicate => mimeTypePredicateFactory((descriptor) => {
    if (descriptor.type !== type) return false;
    if (subtype != null && descriptor.subtype !== subtype) return false;
    return true;
});

// Creating a set of named predicates that will help us determine how to handle different mime-types
const isTextLikeMimeType = mimeTypeSimplePredicateFactory('text');
const isJsonMimeType = mimeTypeSimplePredicateFactory('application', 'json');
const isJsonLikeMimeType = mimeTypePredicateFactory((descriptor) => descriptor.type === 'application' && descriptor.subtypeTokens.some((item) => item === 'json'));
const isOctetStreamMimeType = mimeTypeSimplePredicateFactory('application', 'octet-stream');
const isFormUrlencodedMimeType = mimeTypeSimplePredicateFactory('application', 'x-www-form-urlencoded');

// Defining a list of mime-types in the order of prioritization for handling.
const supportedMimeTypePredicatesWithPriority: MimeTypePredicate[] = [
    isJsonMimeType,
    isJsonLikeMimeType,
    isTextLikeMimeType,
    isOctetStreamMimeType,
    isFormUrlencodedMimeType,
];

const nullableSuffix = " | null";
const optionalSuffix = " | undefined";
const arrayPrefix = "Array<";
const arraySuffix = ">";
const mapPrefix = "{ [key: string]: ";
const mapSuffix = "; }";

export class ObjectSerializer {
    public static findCorrectType(data: any, expectedType: string) {
        if (data == undefined) {
            return expectedType;
        } else if (primitives.indexOf(expectedType.toLowerCase()) !== -1) {
            return expectedType;
        } else if (expectedType === "Date") {
            return expectedType;
        } else {
            if (enumsMap.has(expectedType)) {
                return expectedType;
            }

            if (!typeMap[expectedType]) {
                return expectedType; // w/e we don't know the type
            }

            // Check the discriminator
            let discriminatorProperty = typeMap[expectedType].discriminator;
            if (discriminatorProperty == null) {
                return expectedType; // the type does not have a discriminator. use it.
            } else {
                if (data[discriminatorProperty]) {
                    var discriminatorType = data[discriminatorProperty];
                    let mapping = typeMap[expectedType].mapping;
                    if (mapping != undefined && mapping[discriminatorType]) {
                        return mapping[discriminatorType]; // use the type given in the discriminator
                    } else if(typeMap[discriminatorType]) {
                        return discriminatorType;
                    } else {
                        return expectedType; // discriminator did not map to a type
                    }
                } else {
                    return expectedType; // discriminator was not present (or an empty string)
                }
            }
        }
    }

    public static serialize(data: any, type: string, format: string): any {
        if (data == undefined) {
            return data;
        } else if (primitives.indexOf(type.toLowerCase()) !== -1) {
            return data;
        } else if (type.endsWith(nullableSuffix)) {
            let subType: string = type.slice(0, -nullableSuffix.length); // Type | null => Type
            return ObjectSerializer.serialize(data, subType, format);
        } else if (type.endsWith(optionalSuffix)) {
            let subType: string = type.slice(0, -optionalSuffix.length); // Type | undefined => Type
            return ObjectSerializer.serialize(data, subType, format);
        } else if (type.startsWith(arrayPrefix)) {
            let subType: string = type.slice(arrayPrefix.length, -arraySuffix.length); // Array<Type> => Type
            let transformedData: any[] = [];
            for (let date of data) {
                transformedData.push(ObjectSerializer.serialize(date, subType, format));
            }
            return transformedData;
        } else if (type.startsWith(mapPrefix)) {
            let subType: string = type.slice(mapPrefix.length, -mapSuffix.length); // { [key: string]: Type; } => Type
            let transformedData: { [key: string]: any } = {};
            for (let key in data) {
                transformedData[key] = ObjectSerializer.serialize(
                    data[key],
                    subType,
                    format,
                );
            }
            return transformedData;
        } else if (type === "Date") {
            if (format == "date") {
                let month = data.getMonth()+1
                month = month < 10 ? "0" + month.toString() : month.toString()
                let day = data.getDate();
                day = day < 10 ? "0" + day.toString() : day.toString();

                return data.getFullYear() + "-" + month + "-" + day;
            } else {
                return data.toISOString();
            }
        } else {
            if (enumsMap.has(type)) {
                return data;
            }
            if (!typeMap[type]) { // in case we dont know the type
                return data;
            }

            // Get the actual type of this object
            type = this.findCorrectType(data, type);

            // get the map for the correct type.
            let attributeTypes = typeMap[type].getAttributeTypeMap();
            let instance: {[index: string]: any} = {};
            for (let attributeType of attributeTypes) {
                instance[attributeType.baseName] = ObjectSerializer.serialize(data[attributeType.name], attributeType.type, attributeType.format);
            }
            return instance;
        }
    }

    public static deserialize(data: any, type: string, format: string): any {
        // polymorphism may change the actual type.
        type = ObjectSerializer.findCorrectType(data, type);
        if (data == undefined) {
            return data;
        } else if (primitives.indexOf(type.toLowerCase()) !== -1) {
            return data;
        } else if (type.endsWith(nullableSuffix)) {
            let subType: string = type.slice(0, -nullableSuffix.length); // Type | null => Type
            return ObjectSerializer.deserialize(data, subType, format);
        } else if (type.endsWith(optionalSuffix)) {
            let subType: string = type.slice(0, -optionalSuffix.length); // Type | undefined => Type
            return ObjectSerializer.deserialize(data, subType, format);
        } else if (type.startsWith(arrayPrefix)) {
            let subType: string = type.slice(arrayPrefix.length, -arraySuffix.length); // Array<Type> => Type
            let transformedData: any[] = [];
            for (let date of data) {
                transformedData.push(ObjectSerializer.deserialize(date, subType, format));
            }
            return transformedData;
        } else if (type.startsWith(mapPrefix)) {
            let subType: string = type.slice(mapPrefix.length, -mapSuffix.length); // { [key: string]: Type; } => Type
            let transformedData: { [key: string]: any } = {};
            for (let key in data) {
                transformedData[key] = ObjectSerializer.deserialize(
                    data[key],
                    subType,
                    format,
                );
            }
            return transformedData;
        } else if (type === "Date") {
            return new Date(data);
        } else {
            if (enumsMap.has(type)) {// is Enum
                return data;
            }

            if (!typeMap[type]) { // dont know the type
                return data;
            }
            let instance = new typeMap[type]();
            let attributeTypes = typeMap[type].getAttributeTypeMap();
            for (let attributeType of attributeTypes) {
                let value = ObjectSerializer.deserialize(data[attributeType.baseName], attributeType.type, attributeType.format);
                if (value !== undefined) {
                    instance[attributeType.name] = value;
                }
            }
            return instance;
        }
    }


    /**
     * Normalize media type
     *
     * We currently do not handle any media types attributes, i.e. anything
     * after a semicolon. All content is assumed to be UTF-8 compatible.
     */
    public static normalizeMediaType(mediaType: string | undefined): string | undefined {
        if (mediaType === undefined) {
            return undefined;
        }
        return (mediaType.split(";")[0] ?? '').trim().toLowerCase();
    }

    /**
     * From a list of possible media types, choose the one we can handle best.
     *
     * The order of the given media types does not have any impact on the choice
     * made.
     */
    public static getPreferredMediaType(mediaTypes: Array<string>): string {
        /** According to OAS 3 we should default to json */
        if (mediaTypes.length === 0) {
            return "application/json";
        }

        const normalMediaTypes = mediaTypes.map(ObjectSerializer.normalizeMediaType);

        for (const predicate of supportedMimeTypePredicatesWithPriority) {
            for (const mediaType of normalMediaTypes) {
                if (mediaType != null && predicate(mediaType)) {
                    return mediaType;
                }
            }
        }

        throw new Error("None of the given media types are supported: " + mediaTypes.join(", "));
    }

    /**
     * Convert data to a string according the given media type
     */
    public static stringify(data: any, mediaType: string): string {
        if (isTextLikeMimeType(mediaType)) {
            return String(data);
        }

        if (isJsonLikeMimeType(mediaType)) {
            return JSON.stringify(data);
        }

        throw new Error("The mediaType " + mediaType + " is not supported by ObjectSerializer.stringify.");
    }

    /**
     * Parse data from a string according to the given media type
     */
    public static parse(rawData: string, mediaType: string | undefined) {
        if (mediaType === undefined) {
            throw new Error("Cannot parse content. No Content-Type defined.");
        }

        if (isTextLikeMimeType(mediaType)) {
            return rawData;
        }

        if (isJsonLikeMimeType(mediaType)) {
            return JSON.parse(rawData);
        }

        throw new Error("The mediaType " + mediaType + " is not supported by ObjectSerializer.parse.");
    }
}
